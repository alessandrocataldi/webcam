const Alert = () => {
  return (
    <div
      id="alert"
      className="bg-red-600 text-cyan-50 text-center font-semibold text-base"
    >
      Connessione assente o lenta, non ricaricare la pagina.
    </div>
  );
};

export default Alert;
